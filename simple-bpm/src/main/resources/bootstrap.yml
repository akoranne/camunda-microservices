debug: false

server:
  port: ${PORT:8280}

management:
  # show all actuator endpoints (only for POC)
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: '*'

spring:
  application:
    name: hello-bpm
